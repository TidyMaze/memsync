{
  "name": "@memsync/cli",
  "version": "0.1.0",
  "description": "Sync your AI coding memory to every agent format",
  "type": "module",
  "bin": {
    "memsync": "./dist/memsync.js"
  },
  "scripts": {
    "build": "bun run build.ts",
    "dev": "bun run src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.6.0",
    "citty": "^0.1.6",
    "gray-matter": "^4.0.3",
    "picocolors": "^1.1.1",
    "remark": "^15.0.1",
    "remark-frontmatter": "^5.0.0",
    "remark-parse": "^11.0.0",
    "unified": "^11.0.5"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "vitest": "^2.1.8"
  }
}
